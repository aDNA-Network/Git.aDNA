# sanitize_deny_content.txt — R8 content deny list for Git.aDNA
#
# Read by: how/standard/hooks/pre-push-sanitize.sh, rule R8 (LAYER_CONTRACT_VERSION 4.2.0)
# Basis:   what/decisions/adr_016_publication_boundary.md — D2 (unpublishable classes),
#          D5 (a path rule cannot express this boundary), D4 (fix-forward) + Amendment A1
#          (the gate's scope). ✅ ADR-016 RATIFIED 2026-08-27 at rev 3 (operator §7.7).
# STATUS:  ✅ LIVE. Installed 2026-09-02, chained behind the gitleaks secret scanner.
#
# FORMAT — one POSIX ERE per line. Blank lines and #-comments are skipped.
#          A malformed pattern BLOCKS the push (fail-closed, ADR-011 A4 §2(a)).
# ESCAPE  — `pragma: allowlist` anywhere on a matching line exempts that line.
# OUTPUT  — matches are reported REDACTED. This file must never be made to print the
#           string it exists to withhold, and neither must a memo about it.
#
# ---------------------------------------------------------------------------
# ⛩ WHY THIS FILE SAT UNINSTALLED FOR FOUR SITTINGS, AND WHAT DISCHARGED IT
# ---------------------------------------------------------------------------
# Kept as the record. Both reasons this file gave for being held back were TRUE when
# written, and neither was the reason it was actually stuck.
#
#   REASON 1 (recorded 2026-08-26) — "installing blocks our own next push, and the
#     resolution requires the operator's ADR-016 ruling first."
#     ⇒ DISCHARGED 2026-08-27: ADR-016 ratified at rev 3.
#
#   REASON 2 (F-P7b-ag, measured 2026-08-27) — "and the HOOK THAT READS THIS FILE IS NOT
#     INSTALLED EITHER." `.git/hooks/pre-push` was the gitleaks scanner; core.hooksPath
#     unset. ⇒ R1–R8 had NEVER run on a real push in this vault.
#     ⇒ DISCHARGED 2026-09-02: installed CHAINED behind the gitleaks scanner (both run,
#       either can block). Neither control was traded for the other.
#
# ⛔ THE REAL BLOCKER WAS NEITHER OF THOSE — IT WAS THE GATE'S OWN SCOPE.
#   This file listed three resolutions to the self-block: (a) an explicit recorded
#   allowlist, (b) ship uninstalled, (c) weaken the pattern until it passes. It shipped
#   (b) for four sittings. ⭐ THE LIST WAS INCOMPLETE, AND THE MISSING ITEM WAS THE ONE
#   THAT NEEDED NOTHING FROM ANYBODY:
#
#     (d) SCOPE THE GATE TO D4'S OWN RULE — check the lines a push would ADD.
#
#   D4 had said it in words since ratification: "The boundary binds the next write. The
#   count stops growing; it does not shrink." A whole-file scan EXCEEDED that. The gate was
#   not too weak to install; it was STRICTER THAN THE DOCTRINE IT ENFORCED, which left only
#   remedies D4 forbids (rewrite) or distrusts (exemption).
#   ⇒ hook 4.2.0 + ADR-016 Amendment A1 (proposed). Measured at the change: 70 matching
#     lines across 23 files, ALL already on origin/master, 24 unpushed commits adding ZERO.
#     The gate installs GREEN WITH NO EXEMPTION OF ANY KIND — honestly, not by allowlist.
#
# ⛔ (a) IS NOW FORBIDDEN, NOT MERELY UNUSED — A1 §2. A file-scoped allowlist would have let
#   a NEW occurrence in STATE.md pass, and STATE.md is this vault's heaviest carrier. That is
#   a gate reporting green on the exact case it exists to catch.
# ⛔ (c) STAYS REFUSED AND STAYS NAMED. A gate tuned to report green is the defect this vault
#   has filed under three different names. THE PATTERNS BELOW ARE UNCHANGED BY ANY OF THIS.
#
# ⚠ Ilmarinen reports the same class one rule over and from the consumer side: R7 has shipped
#   in `.adna/` and never once run, because `--self-test` reimplements R1–R6 and never enters
#   the push-time path. Two vaults, two rules, one defect — a gate whose only exercise is a
#   self-test that does not use the gate's own entry point. ⇒ 4.2.0's new test arms drive the
#   REAL entry point with REAL refs; the self-test is not evidence and is not cited as such.
#
# ---------------------------------------------------------------------------
# Patterns
# ---------------------------------------------------------------------------

# 1. Mesh/overlay host addresses in the RFC1918 private ranges, with or without a port.
#    ADR-016 D2.1 — service-topology reconnaissance, not a credential. Loopback (127/8)
#    and the unspecified address (0.0.0.0) are deliberately NOT matched: both are generic
#    and disclose no host. 192.0.2.0/24 and 198.51.100.0/24 (RFC5737 documentation ranges)
#    are the correct substitutes when an example address is genuinely needed.
#
# ⛔ CORRECTED 2026-08-27 (F-P7b-af) — THE GUARDS EXCLUDED `.` AND THE PATTERN WENT BLIND.
#    Both guards were `[^0-9.]`. Excluding an adjacent DIGIT is correct and is the whole
#    point — it stops `<forge-overlay-addr>` matching inside a longer quad that merely
#    contains it (a leading digit). Excluding an adjacent DOT was never needed for that,
#    and it cost real matches in both directions:
#      · leading  — an address PRECEDED by a dot was invisible. ⭐ MEASURED, not supposed:
#        three files in this tree carry the instance host in a `…ly.<addr>` construct and
#        the shipped pattern missed all three. All three are coordination memos, which is
#        exactly the D3 class ("correspondence is the most likely carrier").
#      · trailing — an address at the END OF A PROSE SENTENCE (`…runs at <addr>.`) was
#        invisible, because `.` is not `[^0-9.]` and the address is not at end-of-line.
#
#    ⇒ Guards are now `[^0-9]` / `([^0-9]|$)`. The change can only ADD matches, never remove
#    them, so it moves in the fail-safe direction — the opposite of resolution (c) below.
#    Controls: how/tests/test_sanitize_content_gate.sh, 8 arms (4 must-match incl. both
#    recovered shapes, 4 must-NOT-match incl. the longer-quad, loopback, 0.0.0.0 and RFC5737).
#
#    ⚠ Exposure re-measured at `b6c070c` under the corrected pattern: **75 occurrences over
#    23 files**, where the shipped pattern reported 62/20 and ADR-016 rev 2 §C3 sized the
#    prospective allowlist to 62. The allowlist figure has now moved three times (31 → 62 →
#    75) — which is why ADR-016 rev 3 cites this instrument instead of transcribing a number.
(^|[^0-9])(10\.[0-9]{1,3}\.[0-9]{1,3}\.[0-9]{1,3}|192\.168\.[0-9]{1,3}\.[0-9]{1,3}|172\.(1[6-9]|2[0-9]|3[01])\.[0-9]{1,3}\.[0-9]{1,3})([^0-9]|$)

# 2. Private-key material. R2 already covers the standard PEM header; this catches the
#    OpenSSH public-key form, which R2 does not, and which names a host in its comment.
(^|[[:space:]])ssh-(rsa|ed25519|dss)[[:space:]]+AAAA[A-Za-z0-9+/]{20,}
