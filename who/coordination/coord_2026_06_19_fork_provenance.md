---
type: coordination
created: 2026-06-19
updated: 2026-06-19
status: active
last_edited_by: agent_stanley
tags: [coordination, git, genesis, fork_provenance, router_row, operation_free_harbor]
---

# Coordination — Git.aDNA Fork Provenance & Staged Router Row

## Fork provenance (audit trail)

- **2026-06-19** — `Git.aDNA/` genesis'd from `.adna/` (template fork structure copied in-place) per the approved plan `~/.claude/plans/please-read-the-claude-md-buzzing-treehouse.md`. **Direct-authored, not onboarded** (`MANIFEST.md last_edited_by: agent_stanley`; onboarding suppressed — Warp/Obsidian precedent).
- **Pre-genesis state**: a seed-staging directory (the 2026-06-11 *Lighthouse.aDNA* seed set in `files/`, plus a mislabeled `AGENTS.md` **zip blob** + `files.zip`). Triaged: zips preserved as intake provenance at `how/campaigns/campaign_git_genesis/_seed_intake/`; seed docs folded into `what/` with reframe banners; inherited template ADRs archived → `what/decisions/_archive/`.
- **Naming**: `Git.aDNA` is the **ADR-009 §4 PascalCase operator-override** at the workspace tier (all siblings are `<Name>.aDNA`); the fork skill's snake_case validation applies to the base name, not the workspace convention.
- **Identity**: category **Framework.aDNA (proposed reframe** from the router's `Platform.aDNA (provisional)`); persona **Grace Hopper** (working-pin); codename **Operation Free Harbor** — all `proposed`, ratified at the P0 charter gate. See [[adr_000_project_identity]].

## Router row — ✅ INSERTED 2026-06-19 (operator-authorized)

> The Git.aDNA row in root `~/aDNA/CLAUDE.md` was **replaced** (Project Discovery line 105 + Workspace Layout) and Git.aDNA **added to the Framework Ecosystem active list** on 2026-06-19 — **operator-authorized** override of the Production Tidy freeze (Warp precedent). The edit is **done**. **`#needs-human`: Hestia to append Git.aDNA's Platform→Framework reframe to the PT `disposition_ledger_v3` at the next PT touch** (ledger-reconciliation follow-up only; the router itself is already updated). The exact inserted text is recorded below.

**Project Discovery table (replaces the existing `Git.aDNA/` row):**

```
| `Git.aDNA/` | **Framework.aDNA** (Grace Hopper; display "Git") — platform-agnostic **git/forge/CI-CD** standard every `.aDNA` code-home federates against via a `git/` wrapper; provider abstraction over GitHub + Codeberg + self-hosted Forgejo (**Codeberg-private / GitHub-public** default; Forgejo backend covers both hosted Codeberg + self-hosted); self-hosted **mesh-git** = strategic north star; latent Platform/forge dimension → future `Lighthouse.aDNA`. Genesis P0 (Operation Free Harbor). State: `Git.aDNA/STATE.md`. | `Git.aDNA/CLAUDE.md` |
```

**Workspace Layout line (replaces the existing `Git.aDNA/` line):**

```
├── Git.aDNA/                # Framework.aDNA — platform-agnostic git/forge/CI-CD standard (Grace Hopper); provider abstraction (GitHub+Codeberg+self-hosted Forgejo); Codeberg-private/GitHub-public; mesh-git north star; genesis P0 (Operation Free Harbor)
```

**Ecosystem-section touch** (Framework Ecosystem active list): add Git.aDNA (Hopper; genesis) alongside III.

## Notes
- If the operator **rejects** the Framework reframe at the gate, the row keeps `Platform.aDNA` and the CLAUDE/MANIFEST "Framework" language is re-cast to Platform — a cheap pre-ratification revision.
